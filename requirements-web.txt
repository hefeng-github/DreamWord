# Web界面基础依赖（必需）
flask>=3.0.0
werkzeug>=3.0.0
opencv-python>=4.8.0
pillow>=10.0.0
numpy>=1.24.0

# OCR功能（可选，用于自动查词和自动抄写）
# 代码兼容 PaddleOCR 2.x 与 3.x（PP-OCRv5/v6）。如需启用，取消下面的注释。
# 注意：PaddlePaddle 3.0 仅支持 Python 3.9–3.13，不支持 3.14。
#
# CPU 版（无 N 卡时用）：
# paddlepaddle>=3.0.0
# paddleocr>=3.0.0
#
# GPU 版（需要 NVIDIA 显卡 + CUDA，与 CPU 版二选一）：
#   pip install paddlepaddle-gpu==3.0.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
# paddlepaddle-gpu>=3.0.0
# paddleocr>=3.0.0

# 手写体渲染（可选）
# handright>=1.2.0

# AI语义匹配（可选，用于单词查询）
# sentence-transformers>=2.2.0
# torch>=2.0.0

# 串口通信（可选）
# pyserial>=3.5
